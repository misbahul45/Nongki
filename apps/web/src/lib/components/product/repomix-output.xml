This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
flow/
  product-flow.types.ts
  product-flow.utils.ts
  ProductFlowArrowLayer.svelte
  ProductFlowCanvas.svelte
  ProductFlowNode.svelte
  ProductMobileFlow.svelte
ProductBenefitSection.svelte
ProductCapabilityGrid.svelte
ProductCtaSection.svelte
ProductDetailPage.svelte
ProductHero.svelte
ProductIndexPage.svelte
ProductIntegrationSection.svelte
ProductMockup.svelte
ProductPreviewSection.svelte
ProductProblemSection.svelte
ProductWorkflowSection.svelte
RelatedProductsSection.svelte
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="flow/product-flow.types.ts">
import type { Component } from 'svelte';

export type ProductFlowTone = 'primary' | 'secondary' | 'accent' | 'muted';
export type ProductFlowDisplay = 'canvas' | 'simpleGrid';
export type ProductFlowNodeVariant = 'normal' | 'compact' | 'titleOnly';

export type ProductFlowNode = {
	id: string;
	title: string;
	description?: string;
	eyebrow?: string;
	icon?: Component;
	tone?: ProductFlowTone;
	metrics?: string[];
};

export type ProductFlowConnector = {
	id: string;
	from: string;
	to: string;
	label?: string;
	tone?: ProductFlowTone;
	path: string;
	head: {
		x: number;
		y: number;
		rotate: number;
	};
	dot: {
		x: number;
		y: number;
	};
};

export type ProductFlowLayoutNode = {
	id: string;
	className: string;
};

export type ProductFlowCanvasData = {
	title?: string;
	description?: string;
	viewBox: string;
	minHeightClass: string;
	display?: ProductFlowDisplay;
	nodeVariant?: ProductFlowNodeVariant;
	nodes: ProductFlowNode[];
	layout: ProductFlowLayoutNode[];
	connectors: ProductFlowConnector[];
};
</file>

<file path="flow/product-flow.utils.ts">
import type { ProductFlowCanvasData, ProductFlowTone } from './product-flow.types';

export function iconToneClass(tone: ProductFlowTone = 'primary') {
	if (tone === 'secondary') return 'bg-secondary text-secondary-foreground';
	if (tone === 'accent') return 'bg-accent text-accent-foreground';
	if (tone === 'muted') return 'bg-muted text-foreground';
	return 'bg-primary text-primary-foreground';
}

export function metricToneClass(tone: ProductFlowTone = 'primary') {
	if (tone === 'secondary') return 'bg-secondary/20 text-foreground';
	if (tone === 'accent') return 'bg-accent/10 text-accent';
	if (tone === 'muted') return 'bg-muted/60 text-foreground';
	return 'bg-primary/10 text-primary';
}

export function strokeToneClass(tone: ProductFlowTone = 'primary') {
	if (tone === 'secondary') return 'stroke-secondary';
	if (tone === 'accent') return 'stroke-accent';
	if (tone === 'muted') return 'stroke-muted-foreground';
	return 'stroke-primary';
}

export function fillToneClass(tone: ProductFlowTone = 'primary') {
	if (tone === 'secondary') return 'fill-secondary';
	if (tone === 'accent') return 'fill-accent';
	if (tone === 'muted') return 'fill-muted-foreground';
	return 'fill-primary';
}

export function flowLabelStyle(flow: ProductFlowCanvasData, x: number, y: number) {
	const [, , width = '1000', height = '760'] = flow.viewBox.split(' ');
	const left = (x / Number(width)) * 100;
	const top = (y / Number(height)) * 100;

	return `left:${left}%;top:${top}%;`;
}
</file>

<file path="flow/ProductFlowArrowLayer.svelte">
<script lang="ts">
	import { fillToneClass, strokeToneClass } from './product-flow.utils';
	import type { ProductFlowCanvasData } from './product-flow.types';

	let {
		flow,
		arrowSvgEl = $bindable()
	}: {
		flow: ProductFlowCanvasData;
		arrowSvgEl?: SVGSVGElement;
	} = $props();
</script>

<svg
	bind:this={arrowSvgEl}
	class="pointer-events-none absolute inset-0 z-10 h-full w-full"
	viewBox={flow.viewBox}
	preserveAspectRatio="none"
	aria-hidden="true"
>
	{#each flow.connectors as connector (connector.id)}
		<path
			data-flow-path
			d={connector.path}
			class={strokeToneClass(connector.tone)}
			fill="none"
			stroke-width={connector.tone === 'accent' ? '3.5' : '3'}
			stroke-linecap="round"
			stroke-linejoin="round"
		></path>

		<path
			data-flow-head
			d="M 0 -5 L 10 0 L 0 5 z"
			class={fillToneClass(connector.tone)}
			transform={`translate(${connector.head.x} ${connector.head.y}) rotate(${connector.head.rotate})`}
		></path>

		<circle
			data-flow-dot
			cx={connector.dot.x}
			cy={connector.dot.y}
			r={connector.tone === 'accent' ? '5' : '4.5'}
			class={fillToneClass(connector.tone)}
		></circle>
	{/each}
</svg>
</file>

<file path="flow/ProductFlowCanvas.svelte">
<script lang="ts">
	import { onMount, tick } from 'svelte';
	import { productFlowAnimationConfig } from '$lib/constants/product-flow';
	import ProductFlowArrowLayer from './ProductFlowArrowLayer.svelte';
	import ProductFlowNode from './ProductFlowNode.svelte';
	import ProductMobileFlow from './ProductMobileFlow.svelte';
	import { flowLabelStyle } from './product-flow.utils';
	import type { ProductFlowCanvasData } from './product-flow.types';

	let { flow, animated = false }: { flow: ProductFlowCanvasData; animated?: boolean } = $props();

	let canvasEl = $state<HTMLDivElement>();
	let arrowSvgEl = $state<SVGSVGElement>();

	const nodeById = $derived(new Map(flow.nodes.map((node) => [node.id, node])));
	const isSimpleGrid = $derived(flow.display === 'simpleGrid');
	const simpleGridNodes = $derived(
		flow.nodes.length >= 6
			? [
					flow.nodes[0],
					flow.nodes[1],
					flow.nodes[2],
					flow.nodes[5],
					flow.nodes[4],
					flow.nodes[3]
				].filter(Boolean)
			: flow.nodes
	);
	const animation = productFlowAnimationConfig;

	onMount(() => {
		if (!animated) return;

		let ctx: { revert: () => void } | undefined;
		let observer: IntersectionObserver | undefined;

		async function init() {
			await tick();

			if (!canvasEl || (!isSimpleGrid && !arrowSvgEl)) return;

			const canvas = canvasEl;
			const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
			const gsap = (await import('gsap')).default;
			const paths = arrowSvgEl
				? Array.from(arrowSvgEl.querySelectorAll<SVGPathElement>('[data-flow-path]'))
				: [];
			const heads = arrowSvgEl
				? Array.from(arrowSvgEl.querySelectorAll<SVGPathElement>('[data-flow-head]'))
				: [];
			const dots = arrowSvgEl
				? Array.from(arrowSvgEl.querySelectorAll<SVGCircleElement>('[data-flow-dot]'))
				: [];
			const nodes = Array.from(canvas.querySelectorAll<HTMLElement>('[data-flow-node]'));
			const labels = Array.from(canvas.querySelectorAll<HTMLElement>('[data-flow-label]'));

			paths.forEach((path) => {
				const length = path.getTotalLength();
				path.style.strokeDasharray = `${length}`;
				path.style.strokeDashoffset = `${length}`;
			});

			if (prefersReducedMotion) {
				paths.forEach((path) => {
					path.style.strokeDashoffset = '0';
				});

				gsap.set([...heads, ...dots, ...labels, ...nodes], {
					opacity: 1,
					y: 0,
					scale: 1
				});

				return;
			}

			gsap.set(dots, animation.dot.from);
			gsap.set(heads, animation.head.from);
			gsap.set(labels, animation.label.from);
			gsap.set(nodes, animation.node.from);

			let hasPlayed = false;

			function play() {
				if (hasPlayed) return;
				hasPlayed = true;

				ctx = gsap.context(() => {
					const tl = gsap.timeline({
						defaults: {
							ease: animation.ease
						}
					});

					if (isSimpleGrid) {
						tl.to(nodes, {
							...animation.node.to,
							stagger: animation.node.stagger
						});
						return;
					}

					if (nodes[0]) {
						tl.to(nodes[0], {
							...animation.node.to,
							duration: animation.node.firstDuration
						});
					}

					paths.forEach((path, index) => {
						const dot = dots[index];
						const head = heads[index];
						const label = labels[index];
						const nextNode = nodes[index + 1];

						if (dot) {
							tl.to(
								dot,
								{
									...animation.dot.to
								},
								animation.overlap.dot
							);
						}

						tl.to(
							path,
							{
								strokeDashoffset: 0,
								duration:
									index === 2 ? animation.path.verticalDuration : animation.path.defaultDuration
							},
							animation.overlap.path
						);

						if (head) {
							tl.to(
								head,
								{
									...animation.head.to
								},
								animation.overlap.head
							);
						}

						if (label) {
							tl.to(
								label,
								{
									...animation.label.to
								},
								animation.overlap.label
							);
						}

						if (nextNode) {
							tl.to(
								nextNode,
								{
									...animation.node.to
								},
								animation.overlap.nextNode
							);
						}
					});
				}, canvas);
			}

			observer = new IntersectionObserver(
				([entry]) => {
					if (entry?.isIntersecting && entry.intersectionRatio >= animation.playRatio) {
						play();
						observer?.disconnect();
					}
				},
				{
					threshold: [...animation.observerThreshold]
				}
			);

			observer.observe(canvas);
		}

		init();

		return () => {
			observer?.disconnect();
			ctx?.revert();
		};
	});
</script>

<div class="shadow-3d-lg relative overflow-hidden rounded-3xl border-2 bg-background p-5 md:p-8">
	<div
		aria-hidden="true"
		class="pointer-events-none absolute inset-0 bg-[radial-gradient(circle,var(--border)_1px,transparent_1px)] bg-size-[24px_24px] opacity-60"
	></div>

	<div class="relative z-10">
		{#if isSimpleGrid}
			<div bind:this={canvasEl} class="relative hidden lg:block">
				<div class="grid grid-cols-3 gap-x-10 gap-y-10">
					{#each simpleGridNodes as item (item.id)}
						<div data-flow-node>
							<ProductFlowNode {item} variant="titleOnly" />
						</div>
					{/each}
				</div>
			</div>

			<ProductMobileFlow nodes={flow.nodes} variant="titleOnly" />
		{:else}
			<div bind:this={canvasEl} class={`relative hidden ${flow.minHeightClass} lg:block`}>
				<ProductFlowArrowLayer {flow} bind:arrowSvgEl />

				{#each flow.connectors as connector (connector.id)}
					{#if connector.label}
						<div
							data-flow-label
							class="shadow-3d-sm absolute z-20 -translate-x-1/2 -translate-y-1/2 rounded-full border-2 bg-background px-3 py-1 text-[11px] font-bold"
							style={flowLabelStyle(
								flow,
								(connector.dot.x + connector.head.x) / 2,
								(connector.dot.y + connector.head.y) / 2
							)}
						>
							{connector.label}
						</div>
					{/if}
				{/each}

				{#each flow.layout as layoutNode (layoutNode.id)}
					{@const item = nodeById.get(layoutNode.id)}

					{#if item}
						<div data-flow-node class={`absolute z-30 ${layoutNode.className}`}>
							<ProductFlowNode {item} variant={flow.nodeVariant ?? 'normal'} />
						</div>
					{/if}
				{/each}
			</div>

			<ProductMobileFlow nodes={flow.nodes} variant={flow.nodeVariant ?? 'compact'} />
		{/if}
	</div>
</div>
</file>

<file path="flow/ProductFlowNode.svelte">
<script lang="ts">
	import { iconToneClass, metricToneClass } from './product-flow.utils';
	import type { ProductFlowNode, ProductFlowNodeVariant } from './product-flow.types';

	let {
		item,
		compact = false,
		variant
	}: {
		item: ProductFlowNode;
		compact?: boolean;
		variant?: ProductFlowNodeVariant;
	} = $props();

	const tone = $derived(item.tone ?? 'primary');
	const Icon = $derived(item.icon);
	const currentVariant = $derived(variant ?? (compact ? 'compact' : 'normal'));
	const isTitleOnly = $derived(currentVariant === 'titleOnly');
	const isCompact = $derived(currentVariant === 'compact' || compact || isTitleOnly);
</script>

<div
	class={`shadow-3d-sm hover:shadow-3d-lg rounded-3xl border-2 bg-background transition-all duration-300 hover:-translate-y-1 ${
		isTitleOnly
			? 'flex min-h-[150px] flex-col justify-center p-5 text-center'
			: isCompact
				? 'p-4'
				: 'p-5'
	}`}
>
	{#if isTitleOnly}
		<div class={`grid size-12 shrink-0 place-items-center rounded-2xl ${iconToneClass(tone)}`}>
			{#if Icon}
				<Icon class="size-5" />
			{:else}
				<span class="text-sm font-black">{item.title.slice(0, 1)}</span>
			{/if}
		</div>
		<h3 class="mt-4 font-heading text-xl leading-tight font-bold">
			{item.title}
		</h3>
	{:else}
		<div class="flex items-start gap-3">
			<div
				class={`grid shrink-0 place-items-center rounded-2xl ${isCompact ? 'size-10' : 'size-11'} ${iconToneClass(tone)}`}
			>
				{#if Icon}
					<Icon class={isCompact ? 'size-4' : 'size-5'} />
				{:else}
					<span class="text-sm font-black">{item.title.slice(0, 1)}</span>
				{/if}
			</div>

			<div class="min-w-0 flex-1">
				{#if item.eyebrow}
					<p
						class="mb-2 inline-flex rounded-full border-2 bg-muted/40 px-2.5 py-1 text-[11px] font-black text-muted-foreground"
					>
						{item.eyebrow}
					</p>
				{/if}
				<h3 class={`font-heading leading-tight font-bold ${isCompact ? 'text-base' : 'text-xl'}`}>
					{item.title}
				</h3>
			</div>
		</div>

		{#if item.description}
			<p class={`mt-3 leading-relaxed text-muted-foreground ${isCompact ? 'text-sm' : 'text-sm'}`}>
				{item.description}
			</p>
		{/if}

		{#if item.metrics?.length}
			<div class="mt-4 flex flex-wrap gap-2">
				{#each item.metrics as metric (metric)}
					<span class={`rounded-full px-2.5 py-1 text-[11px] font-black ${metricToneClass(tone)}`}>
						{metric}
					</span>
				{/each}
			</div>
		{/if}
	{/if}
</div>
</file>

<file path="flow/ProductMobileFlow.svelte">
<script lang="ts">
	import ProductFlowNode from './ProductFlowNode.svelte';
	import type {
		ProductFlowNode as ProductFlowNodeData,
		ProductFlowNodeVariant
	} from './product-flow.types';

	let {
		nodes,
		variant = 'compact'
	}: { nodes: ProductFlowNodeData[]; variant?: ProductFlowNodeVariant } = $props();
</script>

<div class="grid gap-4 lg:hidden">
	{#each nodes as item, index (item.id)}
		<ProductFlowNode {item} compact {variant} />

		{#if index < nodes.length - 1}
			<div class="flex justify-center">
				<div class="flex h-10 flex-col items-center">
					<div class="h-7 w-0.5 rounded-full bg-primary/50"></div>
					<div
						class="size-0 border-x-[7px] border-t-[9px] border-x-transparent border-t-primary"
					></div>
				</div>
			</div>
		{/if}
	{/each}
</div>
</file>

<file path="ProductBenefitSection.svelte">
<script lang="ts">
	import Reveal from '$lib/components/home/Reveal.svelte';
	import type { ProductPageData } from '$lib/constants/product';

	let { product }: { product: ProductPageData } = $props();
</script>

<section class="bg-muted/40 py-16 md:py-28">
	<div class="mx-auto w-full max-w-7xl px-4">
		<Reveal>
			<h2 class="max-w-3xl font-heading text-3xl leading-tight font-bold md:text-5xl">
				Dampaknya untuk owner.
			</h2>
		</Reveal>

		<div class="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-4">
			{#each product.benefits as benefit, index (benefit.title)}
				<Reveal delay={index * 0.04}>
					<div
						class={`h-full rounded-3xl border-2 p-5 ${
							index === 0
								? 'shadow-3d-primary border-primary bg-primary/5'
								: 'shadow-3d-sm bg-background'
						}`}
					>
						<h3 class="font-heading text-xl font-bold">{benefit.title}</h3>
						<p class="mt-3 text-sm leading-relaxed text-muted-foreground">{benefit.description}</p>
					</div>
				</Reveal>
			{/each}
		</div>
	</div>
</section>
</file>

<file path="ProductCapabilityGrid.svelte">
<script lang="ts">
	import Reveal from '$lib/components/home/Reveal.svelte';
	import type { ProductPageData } from '$lib/constants/product';

	let { product }: { product: ProductPageData } = $props();
</script>

<section class="bg-background py-16 md:py-28">
	<div class="mx-auto w-full max-w-7xl px-4">
		<Reveal>
			<div class="max-w-3xl space-y-4">
				<h2 class="font-heading text-3xl leading-tight font-bold md:text-5xl">
					Yang bisa dilakukan modul ini.
				</h2>
				<p class="text-lg text-muted-foreground">
					Ringkasan kemampuan utama tanpa masuk terlalu teknis.
				</p>
			</div>
		</Reveal>

		<div class="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">
			{#each product.capabilities as capability, index (capability.title)}
				{@const Icon = capability.icon}
				<Reveal delay={index * 0.04}>
					<div
						class="shadow-3d-sm hover:shadow-3d-lg h-full rounded-3xl border-2 bg-card p-5 transition-all duration-300 hover:-translate-y-1"
					>
						<div
							class="grid size-11 place-items-center rounded-2xl bg-primary text-primary-foreground"
						>
							<Icon class="size-5" />
						</div>
						<h3 class="mt-5 font-heading text-xl font-bold">{capability.title}</h3>
						<p class="mt-2 text-sm leading-relaxed text-muted-foreground">
							{capability.description}
						</p>
					</div>
				</Reveal>
			{/each}
		</div>
	</div>
</section>
</file>

<file path="ProductCtaSection.svelte">
<script lang="ts">
	import Reveal from '$lib/components/home/Reveal.svelte';
	import type { ProductPageData } from '$lib/constants/product';

	let { product }: { product: ProductPageData } = $props();
</script>

<section class="bg-primary py-16 text-primary-foreground md:py-28">
	<div class="mx-auto w-full max-w-5xl px-4 text-center">
		<Reveal>
			<div class="space-y-6">
				<h2 class="font-heading text-3xl leading-tight font-bold md:text-5xl">
					Siap mencoba {product.title}
				</h2>
				<p class="mx-auto max-w-2xl text-lg text-primary-foreground/85">
					Mulai hubungkan WhatsApp bisnismu dan lihat bagaimana chat berubah menjadi data, insight,
					dan action.
				</p>
				<div class="flex flex-col justify-center gap-3 sm:flex-row">
					<a
						href="/auth/register"
						class="inline-flex h-12 items-center justify-center rounded-xl border-2 border-background bg-background px-6 font-bold text-primary shadow-[0_5px_0_0_var(--shadow-3d-primary)] transition-all active:translate-y-1"
					>
						Mulai Gratis
					</a>
					<a
						href="/docs"
						class="inline-flex h-12 items-center justify-center rounded-xl border-2 border-primary-foreground/70 px-6 font-bold text-primary-foreground shadow-[0_5px_0_0_var(--shadow-3d-primary)] transition-all active:translate-y-1"
					>
						Lihat Docs
					</a>
				</div>
			</div>
		</Reveal>
	</div>
</section>
</file>

<file path="ProductDetailPage.svelte">
<script lang="ts">
	import type { ProductPageData } from '$lib/constants/product';
	import ProductBenefitSection from './ProductBenefitSection.svelte';
	import ProductCapabilityGrid from './ProductCapabilityGrid.svelte';
	import ProductCtaSection from './ProductCtaSection.svelte';
	import ProductHero from './ProductHero.svelte';
	import ProductIntegrationSection from './ProductIntegrationSection.svelte';
	import ProductPreviewSection from './ProductPreviewSection.svelte';
	import ProductProblemSection from './ProductProblemSection.svelte';
	import ProductWorkflowSection from './ProductWorkflowSection.svelte';
	import RelatedProductsSection from './RelatedProductsSection.svelte';

	let { product }: { product: ProductPageData } = $props();
</script>

<ProductHero {product} />
<ProductProblemSection {product} />
<ProductCapabilityGrid {product} />
<ProductWorkflowSection {product} />
<ProductPreviewSection {product} />
<ProductBenefitSection {product} />
<ProductIntegrationSection {product} />
<RelatedProductsSection {product} />
<ProductCtaSection {product} />
</file>

<file path="ProductHero.svelte">
<script lang="ts">
	import Reveal from '$lib/components/home/Reveal.svelte';
	import type { ProductPageData } from '$lib/constants/product';
	import ProductMockup from './ProductMockup.svelte';

	let { product }: { product: ProductPageData } = $props();

	const proofPills = ['WhatsApp-ready', 'Data-driven', 'Owner-friendly'];
</script>

<section class="bg-background py-16 md:py-28">
	<div
		class="mx-auto grid w-full max-w-7xl gap-10 px-4 lg:grid-cols-[0.95fr_1.05fr] lg:items-center"
	>
		<Reveal>
			<div class="space-y-7">
				<div class="space-y-4">
					<p class="text-sm font-black tracking-[0.18em] text-primary uppercase">
						{product.eyebrow}
					</p>
					<h1
						class="max-w-4xl font-heading text-4xl leading-tight font-bold text-balance md:text-6xl"
					>
						{product.title}
					</h1>
					<p class="max-w-2xl text-lg leading-relaxed text-muted-foreground">
						{product.description}
					</p>
				</div>

				<div class="flex flex-col gap-3 sm:flex-row">
					<a
						href={product.primaryCta.href}
						class="shadow-3d-primary inline-flex h-12 items-center justify-center rounded-xl border-2 border-primary bg-primary px-6 font-bold text-primary-foreground transition-all active:translate-y-1 sm:w-auto"
					>
						{product.primaryCta.label}
					</a>
					<a
						href={product.secondaryCta.href}
						class="shadow-3d inline-flex h-12 items-center justify-center rounded-xl border-2 bg-background px-6 font-bold transition-all active:translate-y-1 sm:w-auto"
					>
						{product.secondaryCta.label}
					</a>
				</div>

				<div class="flex flex-wrap gap-3">
					{#each proofPills as pill (pill)}
						<span class="shadow-3d-sm rounded-2xl border-2 bg-muted/40 px-4 py-2 text-sm font-bold">
							{pill}
						</span>
					{/each}
				</div>
			</div>
		</Reveal>

		<Reveal delay={0.08}>
			<div class="shadow-3d-lg rounded-[2rem] border-2 bg-muted/40 p-4">
				<ProductMockup type={product.preview.type} />
			</div>
		</Reveal>
	</div>
</section>
</file>

<file path="ProductIndexPage.svelte">
<script lang="ts">
	import Reveal from '$lib/components/home/Reveal.svelte';
	import { productIndex, productIndexFlow, productPages } from '$lib/constants/product';
	import ProductFlowCanvas from './flow/ProductFlowCanvas.svelte';
	import ProductMockup from './ProductMockup.svelte';
</script>

<section class="bg-background py-16 md:py-28">
	<div
		class="mx-auto grid w-full max-w-7xl gap-10 px-4 lg:grid-cols-[0.95fr_1.05fr] lg:items-center"
	>
		<Reveal>
			<div class="space-y-6">
				<p class="text-sm font-black tracking-[0.18em] text-primary uppercase">
					{productIndex.eyebrow}
				</p>

				<h1 class="font-heading text-4xl leading-tight font-bold text-balance md:text-6xl">
					{productIndex.title}
				</h1>

				<p class="max-w-2xl text-lg leading-relaxed text-muted-foreground">
					{productIndex.description}
				</p>

				<div class="flex flex-col gap-3 sm:flex-row">
					<a
						href="/auth/register"
						class="shadow-3d-primary inline-flex h-12 items-center justify-center rounded-xl border-2 border-primary bg-primary px-6 font-bold text-primary-foreground transition-all hover:-translate-y-0.5 active:translate-y-1"
					>
						Mulai Gratis
					</a>

					<a
						href="#product-grid"
						class="shadow-3d inline-flex h-12 items-center justify-center rounded-xl border-2 bg-background px-6 font-bold transition-all hover:-translate-y-0.5 active:translate-y-1"
					>
						Lihat Produk
					</a>
				</div>
			</div>
		</Reveal>

		<Reveal delay={0.08}>
			<div class="shadow-3d-lg rounded-[2rem] border-2 bg-muted/40 p-4">
				<ProductMockup type="dashboard" />
			</div>
		</Reveal>
	</div>
</section>

<section id="product-grid" class="bg-muted/40 py-16 md:py-28">
	<div class="mx-auto w-full max-w-7xl px-4">
		<Reveal>
			<div class="max-w-3xl space-y-4">
				<h2 class="font-heading text-3xl leading-tight font-bold md:text-5xl">
					Pilih modul sesuai workflow bisnismu.
				</h2>

				<p class="text-lg text-muted-foreground">
					Mulai dari chat, lanjut ke data customer, order, payment, dan insight owner.
				</p>
			</div>
		</Reveal>

		<div class="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">
			{#each productIndex.products as slug, index (slug)}
				{@const product = productPages[slug]}
				{@const Icon = product.icon}

				<Reveal delay={index * 0.04}>
					<a
						href={`/product/${product.slug}`}
						class="shadow-3d-sm hover:shadow-3d-lg block h-full rounded-3xl border-2 bg-background p-5 transition-all duration-300 hover:-translate-y-1"
					>
						<div
							class="grid size-12 place-items-center rounded-2xl bg-primary text-primary-foreground"
						>
							<Icon class="size-5" />
						</div>

						<h3 class="mt-5 font-heading text-2xl font-bold">
							{product.eyebrow}
						</h3>

						<p class="mt-3 text-sm leading-relaxed text-muted-foreground">
							{product.description}
						</p>

						<p class="mt-5 text-sm font-black text-primary">Lihat detail -&gt;</p>
					</a>
				</Reveal>
			{/each}
		</div>
	</div>
</section>

<section class="bg-background py-16 md:py-28">
	<div class="mx-auto w-full max-w-7xl px-4">
		<Reveal>
			<div class="mx-auto max-w-3xl space-y-4 text-center">
				<h2 class="font-heading text-3xl leading-tight font-bold md:text-5xl">
					Satu ekosistem untuk chat, data, pembayaran, dan insight.
				</h2>

				<p class="text-lg text-muted-foreground">
					WhatsApp jadi pintu masuk, lalu data mengalir sampai owner punya action.
				</p>
			</div>
		</Reveal>

		<Reveal delay={0.08}>
			<div class="mt-10">
				<ProductFlowCanvas flow={productIndexFlow} animated />
			</div>
		</Reveal>
	</div>
</section>

<section class="bg-primary py-16 text-primary-foreground md:py-28">
	<div class="mx-auto w-full max-w-5xl px-4 text-center">
		<Reveal>
			<div class="space-y-6">
				<h2 class="font-heading text-3xl leading-tight font-bold md:text-5xl">
					Mulai ubah WhatsApp jadi sistem bisnis.
				</h2>

				<p class="mx-auto max-w-2xl text-lg text-primary-foreground/85">
					Pilih modul pertama, hubungkan workflow chat, lalu biarkan data pelanggan tersusun rapi.
				</p>

				<a
					href="/auth/register"
					class="inline-flex h-12 items-center justify-center rounded-xl border-2 border-background bg-background px-6 font-bold text-primary shadow-[0_5px_0_0_var(--shadow-3d-primary)] transition-all hover:-translate-y-0.5 active:translate-y-1"
				>
					Mulai Gratis
				</a>
			</div>
		</Reveal>
	</div>
</section>
</file>

<file path="ProductIntegrationSection.svelte">
<script lang="ts">
	import Reveal from '$lib/components/home/Reveal.svelte';
	import { createProductSimpleGridFlow } from '$lib/constants/product-flow';
	import type { ProductPageData } from '$lib/constants/product';
	import ProductFlowCanvas from './flow/ProductFlowCanvas.svelte';

	let { product }: { product: ProductPageData } = $props();

	const integrationFlow = $derived(
		product.integration.items.length >= 5
			? createProductSimpleGridFlow({
					nodes: [
						...product.integration.items.map((item, index) => ({
							id: `module-${index + 1}`,
							title: item,
							icon: product.icon,
							tone:
								index % 3 === 1
									? ('secondary' as const)
									: index % 3 === 2
										? ('accent' as const)
										: ('primary' as const)
						})),
						...(product.integration.items.length === 5
							? [
									{
										id: 'module-output',
										title: 'Owner Action',
										icon: product.icon,
										tone: 'secondary' as const
									}
								]
							: [])
					]
				})
			: undefined
	);
</script>

<section class="bg-background py-16 md:py-28">
	<div class="mx-auto w-full max-w-7xl px-4">
		<Reveal>
			<div class="max-w-3xl space-y-4">
				<h2 class="font-heading text-3xl leading-tight font-bold md:text-5xl">
					{product.integration.title}
				</h2>
				<p class="text-lg text-muted-foreground">{product.integration.description}</p>
			</div>
		</Reveal>

		<Reveal delay={0.08}>
			<div class="mt-10">
				{#if integrationFlow}
					<ProductFlowCanvas flow={integrationFlow} animated />
				{:else}
					<div
						class="shadow-3d-lg relative overflow-hidden rounded-3xl border-2 bg-background p-5 md:p-7"
					>
						<div
							aria-hidden="true"
							class="pointer-events-none absolute inset-0 bg-[radial-gradient(circle,var(--border)_1px,transparent_1px)] bg-size-[24px_24px] opacity-50"
						></div>

						<div class="relative z-10 hidden items-stretch gap-3 lg:flex">
							{#each product.integration.items as item, index (item)}
								<div
									class={`shadow-3d-sm flex min-h-24 min-w-0 flex-1 flex-col justify-center rounded-2xl border-2 px-4 py-3 ${
										index === 0 ? 'border-primary bg-primary/5' : 'bg-background'
									}`}
								>
									<p class="text-[11px] font-black tracking-[0.14em] text-primary uppercase">
										{index === 0 ? 'Current flow' : `Module ${index + 1}`}
									</p>
									<p class="mt-2 font-heading text-lg leading-tight font-bold">{item}</p>
								</div>

								{#if index < product.integration.items.length - 1}
									<div class="grid shrink-0 place-items-center text-2xl font-black text-primary">
										-&gt;
									</div>
								{/if}
							{/each}
						</div>

						<div class="relative z-10 grid gap-3 lg:hidden">
							{#each product.integration.items as item, index (item)}
								<div
									class={`shadow-3d-sm rounded-2xl border-2 px-4 py-3 ${
										index === 0 ? 'border-primary bg-primary/5' : 'bg-background'
									}`}
								>
									<p class="text-[11px] font-black tracking-[0.14em] text-primary uppercase">
										{index === 0 ? 'Current flow' : `Module ${index + 1}`}
									</p>
									<p class="mt-2 font-heading text-lg leading-tight font-bold">{item}</p>
								</div>

								{#if index < product.integration.items.length - 1}
									<div class="text-center text-2xl font-black text-primary">↓</div>
								{/if}
							{/each}
						</div>
					</div>
				{/if}
			</div>
		</Reveal>
	</div>
</section>
</file>

<file path="ProductMockup.svelte">
<script lang="ts">
	import type { ProductMockupType } from '$lib/constants/product';

	let { type }: { type: ProductMockupType } = $props();
</script>

<div class="shadow-3d-lg w-full overflow-hidden rounded-[2rem] border-2 bg-background p-5">
	{#if type === 'agent'}
		<div class="space-y-4">
			<div class="flex items-center justify-between border-b-2 pb-4">
				<div>
					<p class="font-heading text-xl font-bold">Ningki AI</p>
					<p class="text-sm text-muted-foreground">Confidence 92%</p>
				</div>
				<span class="rounded-full bg-primary px-3 py-1 text-xs font-bold text-primary-foreground"
					>Live</span
				>
			</div>
			<div class="rounded-3xl border-2 bg-muted/40 p-4">
				<p class="text-xs font-bold text-muted-foreground">Customer</p>
				<p class="mt-2 font-bold">Ada menu rice bowl yang cocok buat makan siang?</p>
			</div>
			<div class="ml-8 rounded-3xl border-2 border-primary bg-primary p-4 text-primary-foreground">
				<p class="text-xs font-bold text-primary-foreground/80">AI Reply</p>
				<p class="mt-2 font-bold">
					Ada. Untuk makan siang, Chicken Sambal Matah paling sering dipilih.
				</p>
			</div>
			<div class="flex flex-wrap gap-2">
				<span class="rounded-full border-2 bg-background px-3 py-1 text-xs font-bold">Menu</span>
				<span class="rounded-full border-2 bg-background px-3 py-1 text-xs font-bold">Harga</span>
				<span class="rounded-full border-2 bg-background px-3 py-1 text-xs font-bold"
					>Rekomendasi</span
				>
			</div>
		</div>
	{:else if type === 'chat'}
		<div class="grid min-h-[360px] gap-4 md:grid-cols-[0.82fr_1.18fr]">
			<div class="space-y-3">
				{#each ['Fina', 'Raka', 'Dewi'] as name, index (name)}
					<div
						class={`rounded-2xl border-2 p-3 ${index === 0 ? 'border-primary bg-primary/5' : 'bg-muted/40'}`}
					>
						<div class="flex items-center justify-between gap-3">
							<p class="font-bold">{name}</p>
							<span class="text-xs font-bold text-primary">{index === 0 ? 'Hot' : 'New'}</span>
						</div>
						<p class="mt-1 truncate text-xs text-muted-foreground">Mau tanya paket reservasi...</p>
					</div>
				{/each}
			</div>
			<div class="rounded-3xl border-2 bg-muted/40 p-4">
				<div class="flex items-center justify-between border-b-2 pb-3">
					<div>
						<p class="font-heading text-lg font-bold">Fina</p>
						<p class="text-xs text-muted-foreground">Lead status: Hot</p>
					</div>
					<span class="rounded-full bg-primary px-3 py-1 text-xs font-bold text-primary-foreground"
						>Follow-up</span
					>
				</div>
				<div class="mt-4 space-y-3">
					<div class="max-w-[82%] rounded-2xl border-2 bg-background p-3 text-sm font-bold">
						Ada meja untuk 6 orang?
					</div>
					<div
						class="ml-auto max-w-[82%] rounded-2xl border-2 border-primary bg-primary p-3 text-sm font-bold text-primary-foreground"
					>
						Ada. Mau saya bantu cek jamnya?
					</div>
					<div class="rounded-2xl border-2 bg-background p-3 text-xs text-muted-foreground">
						Summary: tertarik reservasi grup, perlu follow-up hari ini.
					</div>
				</div>
			</div>
		</div>
	{:else if type === 'profile'}
		<div class="space-y-4">
			<div class="flex items-center gap-4 rounded-3xl border-2 bg-primary/5 p-4">
				<div
					class="grid size-16 shrink-0 place-items-center rounded-full bg-primary text-2xl font-black text-primary-foreground"
				>
					F
				</div>
				<div>
					<p class="font-heading text-xl font-bold">Fina Rahma</p>
					<p class="text-sm text-muted-foreground">Last interaction: 12 menit lalu</p>
				</div>
			</div>
			<div class="grid gap-3 sm:grid-cols-2">
				<div class="rounded-2xl border-2 bg-muted/40 p-4">
					<p class="text-xs font-bold text-muted-foreground">Lead score</p>
					<p class="mt-2 font-heading text-3xl font-bold text-primary">86</p>
				</div>
				<div class="rounded-2xl border-2 bg-muted/40 p-4">
					<p class="text-xs font-bold text-muted-foreground">Favorite product</p>
					<p class="mt-2 font-bold">Rice Bowl</p>
				</div>
			</div>
			<div class="rounded-3xl border-2 bg-background p-4">
				<p class="text-xs font-bold text-muted-foreground">Next best action</p>
				<p class="mt-2 font-bold">Kirim promo lunch bundle sebelum jam 11.00.</p>
			</div>
		</div>
	{:else if type === 'payment'}
		<div class="grid gap-4 md:grid-cols-[0.95fr_1.05fr]">
			<div class="space-y-3 rounded-3xl border-2 bg-muted/40 p-4">
				<p class="font-heading text-xl font-bold">Order #NGK-1082</p>
				<div class="flex justify-between border-b-2 pb-2 text-sm">
					<span>Chicken bowl x2</span><b>Rp78k</b>
				</div>
				<div class="flex justify-between border-b-2 pb-2 text-sm">
					<span>Iced tea x2</span><b>Rp24k</b>
				</div>
				<div class="flex justify-between text-base font-black">
					<span>Total</span><span>Rp102k</span>
				</div>
			</div>
			<div class="rounded-3xl border-2 bg-background p-4 text-center">
				<div
					class="mx-auto grid aspect-square max-w-44 grid-cols-5 gap-1 rounded-2xl border-2 bg-muted/40 p-3"
				>
					{#each Array(25) as _, index (index)}
						<span
							class={`rounded-sm ${index % 3 === 0 || index % 7 === 0 ? 'bg-foreground' : 'bg-background'}`}
						></span>
					{/each}
				</div>
				<p class="mt-4 rounded-full bg-primary px-3 py-2 text-sm font-bold text-primary-foreground">
					Waiting payment
				</p>
			</div>
		</div>
	{:else if type === 'dashboard'}
		<div class="space-y-4">
			<div class="grid gap-3 sm:grid-cols-3">
				{#each [{ label: 'Hot lead', value: '18' }, { label: 'Lost order', value: '5' }, { label: 'Asked menu', value: '42' }] as stat (stat.label)}
					<div class="rounded-2xl border-2 bg-muted/40 p-4">
						<p class="text-xs font-bold text-muted-foreground">{stat.label}</p>
						<p class="mt-2 font-heading text-3xl font-bold">{stat.value}</p>
					</div>
				{/each}
			</div>
			<div class="rounded-3xl border-2 bg-primary/5 p-4">
				<p class="font-heading text-xl font-bold">Owner Digest</p>
				<p class="mt-2 text-sm text-muted-foreground">
					Lunch bundle sering ditanya. Ada 7 lead yang belum dibalas lebih dari 1 jam.
				</p>
			</div>
			<div class="space-y-2">
				{#each ['Follow-up Fina untuk reservasi', 'Review promo weekday', 'Cek menu paling sering ditanya'] as insight (insight)}
					<div class="rounded-2xl border-2 bg-background px-4 py-3 text-sm font-bold">
						{insight}
					</div>
				{/each}
			</div>
		</div>
	{:else}
		<div class="space-y-4">
			<div class="rounded-3xl border-2 bg-primary/5 p-4">
				<p class="font-heading text-xl font-bold">Reservasi Fina</p>
				<p class="text-sm text-muted-foreground">Sabtu, 19.00 WIB</p>
			</div>
			<div class="grid gap-3 sm:grid-cols-3">
				<div class="rounded-2xl border-2 bg-muted/40 p-4">
					<p class="text-xs text-muted-foreground">Date</p>
					<b>12 Jun</b>
				</div>
				<div class="rounded-2xl border-2 bg-muted/40 p-4">
					<p class="text-xs text-muted-foreground">Time</p>
					<b>19.00</b>
				</div>
				<div class="rounded-2xl border-2 bg-muted/40 p-4">
					<p class="text-xs text-muted-foreground">Pax</p>
					<b>6 orang</b>
				</div>
			</div>
			<div class="rounded-3xl border-2 bg-background p-4">
				<p class="text-xs font-bold text-muted-foreground">Confirmation</p>
				<p class="mt-2 font-bold">Meja indoor, dekat jendela, status confirmed.</p>
			</div>
			<p
				class="rounded-full bg-primary px-4 py-3 text-center text-sm font-bold text-primary-foreground"
			>
				Confirmed
			</p>
		</div>
	{/if}
</div>
</file>

<file path="ProductPreviewSection.svelte">
<script lang="ts">
	import Reveal from '$lib/components/home/Reveal.svelte';
	import type { ProductPageData } from '$lib/constants/product';
	import ProductMockup from './ProductMockup.svelte';

	let { product }: { product: ProductPageData } = $props();

	const details = ['Realtime context', 'Readable status', 'Action-ready'];
</script>

<section class="bg-background py-16 md:py-28">
	<div
		class="mx-auto grid w-full max-w-7xl gap-10 px-4 lg:grid-cols-[1.05fr_0.95fr] lg:items-center"
	>
		<Reveal>
			<ProductMockup type={product.preview.type} />
		</Reveal>

		<Reveal delay={0.08}>
			<div class="space-y-6">
				<div class="space-y-4">
					<p class="text-sm font-black tracking-[0.18em] text-primary uppercase">Preview</p>
					<h2 class="font-heading text-3xl leading-tight font-bold md:text-5xl">
						{product.preview.title}
					</h2>
					<p class="text-lg text-muted-foreground">{product.preview.description}</p>
				</div>

				<div class="grid gap-3 sm:grid-cols-3">
					{#each details as detail (detail)}
						<div class="shadow-3d-sm rounded-2xl border-2 bg-muted/40 px-4 py-3 text-sm font-bold">
							{detail}
						</div>
					{/each}
				</div>
			</div>
		</Reveal>
	</div>
</section>
</file>

<file path="ProductProblemSection.svelte">
<script lang="ts">
	import Reveal from '$lib/components/home/Reveal.svelte';
	import type { ProductPageData } from '$lib/constants/product';

	let { product }: { product: ProductPageData } = $props();
</script>

<section class="bg-muted/40 py-16 md:py-28">
	<div class="mx-auto w-full max-w-7xl px-4">
		<Reveal>
			<div class="max-w-3xl space-y-4">
				<p class="text-sm font-black tracking-[0.18em] text-primary uppercase">
					Masalah yang dibereskan
				</p>
				<h2 class="font-heading text-3xl leading-tight font-bold md:text-5xl">
					{product.problem.title}
				</h2>
				<p class="text-lg text-muted-foreground">{product.problem.description}</p>
			</div>
		</Reveal>

		<div class="mt-10 grid gap-4 md:grid-cols-2 lg:grid-cols-4">
			{#each product.problem.items as item, index (item)}
				<Reveal delay={index * 0.04}>
					<div class="shadow-3d-sm h-full rounded-3xl border-2 bg-background p-5">
						<p class="text-sm font-black text-primary">0{index + 1}</p>
						<p class="mt-4 text-base font-bold">{item}</p>
					</div>
				</Reveal>
			{/each}
		</div>
	</div>
</section>
</file>

<file path="ProductWorkflowSection.svelte">
<script lang="ts">
	import Reveal from '$lib/components/home/Reveal.svelte';
	import { productWorkflowFlows, type ProductPageData } from '$lib/constants/product';
	import ProductFlowCanvas from './flow/ProductFlowCanvas.svelte';

	let { product }: { product: ProductPageData } = $props();

	const canvasFlow = $derived(productWorkflowFlows[product.slug]);
</script>

<section class="bg-primary/5 py-16 md:py-28">
	<div class="mx-auto w-full max-w-7xl px-4">
		<Reveal>
			<div class="mx-auto max-w-3xl space-y-4 text-center">
				<h2 class="font-heading text-3xl leading-tight font-bold md:text-5xl">
					{canvasFlow?.title ?? product.workflow.title}
				</h2>
				<p class="text-lg text-muted-foreground">
					{canvasFlow?.description ?? product.workflow.description}
				</p>
			</div>
		</Reveal>

		<Reveal delay={0.08}>
			<div class="mt-10">
				{#if canvasFlow}
					<ProductFlowCanvas flow={canvasFlow} animated />
				{/if}
			</div>
		</Reveal>
	</div>
</section>
</file>

<file path="RelatedProductsSection.svelte">
<script lang="ts">
	import Reveal from '$lib/components/home/Reveal.svelte';
	import { productPages, type ProductPageData } from '$lib/constants/product';

	let { product }: { product: ProductPageData } = $props();

	const relatedProducts = $derived(product.related.slice(0, 3).map((slug) => productPages[slug]));
</script>

<section class="bg-primary/5 py-16 md:py-28">
	<div class="mx-auto w-full max-w-7xl px-4">
		<Reveal>
			<h2 class="font-heading text-3xl leading-tight font-bold md:text-5xl">
				Lanjutkan ke modul terkait.
			</h2>
		</Reveal>

		<div class="mt-10 grid gap-5 md:grid-cols-3">
			{#each relatedProducts as related, index (related.slug)}
				{@const Icon = related.icon}
				<Reveal delay={index * 0.04}>
					<a
						href={`/product/${related.slug}`}
						class="shadow-3d-sm hover:shadow-3d-lg block h-full rounded-3xl border-2 bg-background p-5 transition-all duration-300 hover:-translate-y-1"
					>
						<div
							class="grid size-11 place-items-center rounded-2xl bg-primary text-primary-foreground"
						>
							<Icon class="size-5" />
						</div>
						<h3 class="mt-5 font-heading text-xl font-bold">{related.eyebrow}</h3>
						<p class="mt-2 line-clamp-3 text-sm leading-relaxed text-muted-foreground">
							{related.description}
						</p>
					</a>
				</Reveal>
			{/each}
		</div>
	</div>
</section>
</file>

</files>
